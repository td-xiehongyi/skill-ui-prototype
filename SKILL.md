---
name: ui-prototype
description: "Use when a user wants to explore frontend page layouts, create or revise an interactive HTML prototype before implementation, or compare a running UI against an approved prototype. 适用于页面构想、HTML 原型、设计版本确认和 UI 对照验收；不用于普通文案修改或无设计对照需求的后端任务。"
---

# UI Prototype 界面原型与对照验收

把用户要完成的任务变成可查看、可操作、可确认的 HTML，并让后续实现有稳定的视觉与交互依据。默认中文沟通，遵循用户指定的语言和设计。

## 起点与范围

先读用户材料、项目规则、相关需求、已有设计及本次涉及的实现，保留已有改动。已有 project-kickoff 记录时直接提取目标用户、核心任务、确认范围、约束和未决问题；没有该 Skill 也能独立工作，不要求补做完整项目启动。

| 用户意图 | 工作模式 | 停止点 |
| --- | --- | --- |
| 构想页面、先做 HTML、调整原型 | 设计原型 | 交付可评审草案，或记录用户确认的版本 |
| 检查实际 UI 是否符合已确认设计 | 对照验收 | 交付差异、证据及未验证项 |

原型请求授权制作独立演示文件，不自动授权正式前端改造、后端接入、安装或发布。已明确授权的范围直接执行，无需重复询问。验收中发现问题时，按现有修复授权处理；没有修复授权就提供具体差异。

## 设计原型

1. **确定页面要解决什么。** 梳理核心操作路径、页面或区域、主要信息和目标窗口。已有答案不重问。目标用户、业务目的或必要业务规则缺失时，先问影响最大的一个问题，等待回答后再做依赖它的设计。目标已清楚而交互顺序或布局尚未定时，进入“探索方案”：把待验证判断标为假设，制作可比较的候选原型，不要求用户先选定要用原型验证的答案。已有明确设计则进入“落实设计”。纯视觉偏好缺失时可采用标明的默认风格。
2. **确定视觉依据。** 用户给定截图、设计稿或已确认版本时先查看，并以其为依据。仅在布局方向确实未定或用户不满意时，展示两到三个有明显结构差异的方案；已有明确方向不强制选型。解释信息层级、操作路径和空间分配的取舍。
3. **制作独立 HTML。** 优先使用本地 HTML、CSS 和少量 JavaScript，避免为演示引入构建体系。沿项目已有目录组织；无约定时放在 `docs/ui-prototype/`。用真实感的虚构数据演示主要操作及反馈，清楚标注模拟范围；文件选择、登录、AI 和写入操作均不连接真实能力。原型不包含密钥，不修改真实业务数据。用户明确要求真实集成时，将其作为单独的实施范围处理。
4. **覆盖状态和尺寸。** 根据页面实际需要展示正常、空白、加载、失败、禁用、长文本及窄窗口。提供便于复现的状态入口。主要按钮有可观察反馈；未演示的动作明确标注。使用语义化控件、可见焦点和基本键盘操作。窄窗口策略按产品目标决定，不擅自把桌面产品扩成移动端项目。
5. **检查后展示。** 使用可用浏览器实际打开，走通关键路径并检查目标尺寸、溢出、弹层与关键状态，修复观察到的问题。记录实际环境和证据；只能读源码时，明确“未进行浏览器运行和视觉检查”，仍交付可查看文件及打开方法。提供文件链接；有可用预览工具时打开供用户查看。
6. **迭代与确认。** 在工作草案上完成已授权调整；每次交付评审时保存带编号的不可覆盖快照，并明确展示该编号。草案交付、浏览器检查通过、用户确认是三个不同状态。确认绑定用户实际看过的评审编号和具体范围；对象不清时仅澄清指代，历史材料缺失时标明不能重建基准，不能用当前草案代替。沉默或“继续看看”不算确认。

## 交付与版本

准备交付或记录确认时读取 [交付约定](references/deliverables.md)。最小交付包含可交互 HTML、集中定义的样式变量、关键状态与交互规则，以及对应的验收条目。小原型可合并说明文件，样式变量可内联在 HTML 中，不为了凑文件拆分。

确认时记录版本、来源评审编号、路径、分项确认范围、用户确认依据及未决项，保留当时的 HTML 和本地依赖。部分确认只适用于列明的页面、区域或流程，不能扩展到整版；新草案有变更的部分重新标为待确认，未变部分可引用原确认依据。后续草案从新版本继续，不能通过覆盖已确认原型来消除实现差异。主要布局或交互变更需展示具体差异并取得相应确认，沿用已有明确授权。

交回 project-kickoff 或开发阶段时，提供原型入口与版本、确认状态、交互规则、模拟范围、新发现及受影响需求。新增业务规则标为待确认；视觉确认不等于业务需求、开发阶段或真实后端已获批准。

## 对照验收

使用此模式时读取 [对照验收](references/acceptance.md)。定位真实可访问的已确认基准及确认记录，再在相同尺寸、数据和状态下比较实际运行 UI。没有基准时，只能做草案比较或一般界面检查，不能声称符合已确认设计。

交付逐项结论、可复现步骤、证据与未验证项。构建通过不证明视觉一致，原型模拟成功不证明正式业务可用。对照完成后在本次授权范围结束。

## 维护

创建或修改本 Skill 时用 [行为场景](references/behavior-tests.md) 检查触发、交付完整性和阶段边界。格式检查、情景模拟、真实项目试用和安装后的触发验证分别记录。
