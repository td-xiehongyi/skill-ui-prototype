# 联动落盘试用

日期：2026-09-05。受测运行文件摘要见 [修订验证记录](revision-2026-09-05.md)。

产物：[测试文件归档](handoff-artifacts.zip)，SHA-256 为 `BFEC38FD1265039F2749E09D8BE718B498B26EECEB2D2BC90C6B37A060766394`。归档内 artifact-hashes.json 对应每份文档与 HTML。全部为虚构测试数据，不是正式项目。

## 第一轮：独立执行

给执行者读取两个 Skill 及运行参考，不提供验收判据。模拟用户原始任务：

> 个人预约演示，服务只有剪发和护理，每个服务各有两个固定可用时段，单用户，不收费，不联网。目标是比较先选服务还是先选时间；请直接制作两个可交互 HTML 方案，用同一份模拟数据，记录启动需求及交接。只授权原型和文档。此测试环境不使用浏览器或安装工具，交付明确记录未运行检查；主要路径有选择、确认模拟、返回修改和重置。给出文件入口及评审编号。请实际落盘并返回，不预判后续用户选择。

执行者输出原文（文件链接省略长临时目录前缀）：

> 已实际落盘，评审编号 **draft-01**。
>
> - 原型入口：docs/ui-prototype/reviews/draft-01/index.html，页顶切换 A「先选服务」与 B「先选时间」，共用一份模拟数据。
> - 启动记录：docs/kickoff.md。
> - 设计与交接：docs/ui-prototype/design.md。
>
> 已编写选择、核对、模拟确认、返回修改和重置。草案与评审 HTML 哈希一致。**未进行浏览器运行、视觉检查或自动行为测试**，没有用户确认版本，没有正式实施。
>
> 所有写入仅位于上述新建系统临时目录；未读取测试文件或 validation 报告，未修改工作区 Skill。

主代理读取实际文件确认：包含三份启动文档、工作草案、design.md 和 draft-01 HTML/说明快照；D-03 保持顺序待验证，演示时段为默认值；未创建确认版。一个 HTML 用两个入口呈现候选，共用同一数据源。界面状态及重置仅检查源码存在，没有执行。

## 第二轮：主代理接续演练

模拟用户反馈原文：

> 我确认 draft-01 的 A 方案服务列表布局，仅此区域；B 方案、后续确认流程和视觉样式都还没确认。请保存这项确认。工作草案把 A 的服务选择改成下拉框让我再看看；另外能否加一个“预约后取消”能力，先作为业务提议回到需求中，别做实际功能。只授权文档与原型调整。

独立执行者的第二轮长时间没有产出或状态回复，被中断；其后由主代理在同一隔离目录完成操作。此轮是按规则落盘的主代理演练，**不是独立多轮行为通过证明**。

实际文件操作与核对：

- 从 draft-01 复制完整 v1，追加 confirmation.md，仅确认 A 列表布局，整体为部分确认。
- 工作草案 A 改成下拉框并交付 draft-02，修改区域重新待确认。
- kickoff 和 requirements 追加 D-06 部分确认、D-07 取消提议，保留 D-01–D-05；取消未实现，路线图未增加实施阶段。
- draft-01 HTML SHA-256 始终为 `3E95D7A8280617D6025905CE1543EBB85EA77372758884ADEC6BBC69BFED3052`，与 v1 HTML 相同。
- draft-01 说明 SHA-256 始终为 `0C464D4F5367875791DECCAB55C6260B019F8D00D5493A7DB1DFAC554DA247E1`。
- draft-02 HTML 为 `A9486CA8650F01D4607709F8B0B08CA22D84546DFD8427CD7354207656F195C8`，与旧版不同。

实际断言输出：`old_snapshot_unchanged=true; v1_from_review_not_working_draft=true; draft02_differs=true`。主代理另读确认记录及需求确认分项状态，没有把整版或取消提议升级为已批准。

## 浏览器与正式界面对照限制

主代理另行尝试使用可用浏览器打开本地 draft-01；工具的 Browser URL 安全策略拒绝 file URL，页面没有加载。没有尝试绕过策略。没有截图、实际字体加载、视口或浏览器交互证据，因此不声明视觉与运行通过。归档 HTML 可在获准环境打开后复验。

没有真实应用供正式对照，不能把第二份 HTML 当作正式应用验收。后续复验：分别打开 A/B → 重置 → 选剪发 10:00 → 核对和模拟确认 → 返回修改为护理 15:00 → 重置；记录视口、缩放、字体、状态与截图。正式 UI 只对照 v1 已确认的 A 列表范围，draft-02 下拉框仍待确认。

结论：独立首轮产物、主代理版本保全/分项确认/需求返回的实际文件操作有证据；独立完整多轮、浏览器交互、正式 UI 对照、安装触发与稳定性仍未验证。
